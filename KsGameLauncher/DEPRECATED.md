# Deprecated

This `.NET Framework 4.8` project is deprecated and no longer builds with the current SDK toolchain.

The maintained application is now [`src/KsGameLauncher2`](../src/KsGameLauncher2), a rewrite targeting `.NET 10`.
This project is kept for historical reference only and is excluded from the solution's default build
(see `KsGameLauncher.sln`).
